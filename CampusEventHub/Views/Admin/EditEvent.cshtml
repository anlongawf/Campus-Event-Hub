@model CampusEventHub.Models.Event

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <title>Chỉnh sửa sự kiện</title>
</head>
<body class="bg-light">
<div class="container my-5">
    <div class="card shadow-lg">
        <div class="card-header bg-primary text-white">
            <h3 class="mb-0">✏️ Chỉnh sửa sự kiện</h3>
        </div>
        <div class="card-body">

            @if (Model.Seats != null && Model.Seats.Any())
            {
                <div class="alert alert-info">
                    <strong>📊 Thông tin ghế:</strong><br />
                    Tổng số ghế: <strong>@Model.Seats.Count</strong> |
                    <span class="text-success">Còn trống: @Model.Seats.Count(s => s.Status == CampusEventHub.Models.SeatStatus.Available)</span> |
                    <span class="text-danger">Đã đặt: @Model.Seats.Count(s => s.Status == CampusEventHub.Models.SeatStatus.Booked)</span> |
                    <span class="text-warning">Bị khóa: @Model.Seats.Count(s => s.Status == CampusEventHub.Models.SeatStatus.Reserved)</span>
                </div>
            }

            <form asp-action="EditEvent" asp-controller="Admin" method="post">
                <input type="hidden" asp-for="EventId" />

                <div class="mb-3">
                    <label asp-for="EventName" class="form-label">Tên sự kiện *</label>
                    <input asp-for="EventName" class="form-control" placeholder="Nhập tên sự kiện" />
                    <span asp-validation-for="EventName" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Description" class="form-label">Mô tả</label>
                    <textarea asp-for="Description" class="form-control" rows="3" placeholder="Nhập mô tả chi tiết"></textarea>
                    <span asp-validation-for="Description" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Location" class="form-label">Địa điểm</label>
                    <input asp-for="Location" class="form-control" placeholder="Nhập địa điểm tổ chức" />
                    <span asp-validation-for="Location" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="EventDate" class="form-label">Ngày giờ diễn ra *</label>
                    <input asp-for="EventDate" type="datetime-local" class="form-control" />
                    <span asp-validation-for="EventDate" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="TrainningPoint" class="form-label">Điểm rèn luyện</label>
                    <input asp-for="TrainningPoint" type="number" min="0" class="form-control" placeholder="Nhập điểm rèn luyện" />
                    <span asp-validation-for="TrainningPoint" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="ImageUrl" class="form-label">URL hình ảnh</label>
                    <input asp-for="ImageUrl" type="text" class="form-control" placeholder="https://example.com/image.jpg" id="imageUrl" />
                    <span asp-validation-for="ImageUrl" class="text-danger"></span>

                    @if (!string.IsNullOrEmpty(Model.ImageUrl))
                    {
                        <div class="mt-3">
                            <img src="@Model.ImageUrl" alt="Event Image" id="previewImage" class="img-fluid rounded shadow-sm" style="max-height:200px;" />
                        </div>
                    }
                </div>

                <div class="d-flex justify-content-between">
                    <a href="@Url.Action("Index", "Admin")" class="btn btn-secondary">
                        <i class="fa fa-arrow-left"></i> Hủy
                    </a>
                    <button type="submit" class="btn btn-success">
                        💾 Lưu thay đổi
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script>
    // Preview image khi thay đổi URL
    document.getElementById('imageUrl')?.addEventListener('input', function(e) {
        const url = e.target.value;
        const preview = document.getElementById('previewImage');
        if (preview && url) {
            preview.src = url;
            preview.onerror = function() {
                this.style.display = 'none';
            };
            preview.onload = function() {
                this.style.display = 'block';
            };
        }
    });
</script>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
</body>
</html>
